<% best   = @synergy[:best_teammate] %>
<% nem    = @synergy[:nemesis] %>
<% if best || nem %>
  <div class="card shadow-sm mb-5">
    <div class="card-body">
      <h5 class="card-title mb-4">
        <i class="fas fa-users me-2"></i>Conexiones de Juego
      </h5>

      <div class="row text-center">
        <div class="col-md-6 mb-3 mb-md-0">
          <div class="p-3 rounded-4 bg-dark text-light shadow-sm h-100">
            <h6 class="fw-bold mb-2">🤝 Mejor Compañero</h6>
            <% if best %>
              <p class="mb-1 fs-5"><%= best[:player].full_name %></p>
              <small class="text-muted">
                Juntos: <%= best[:wins] %>/<%= best[:total] %> • <%= best[:win_rate] %>% victorias
              </small>
            <% else %>
              <small class="text-muted">Aún no hay suficientes partidos.</small>
            <% end %>
          </div>
        </div>

        <div class="col-md-6">
          <div class="p-3 rounded-4 bg-dark text-light shadow-sm h-100">
            <h6 class="fw-bold mb-2">⚔️ Némesis Personal</h6>
            <% if nem %>
              <p class="mb-1 fs-5"><%= nem[:player].full_name %></p>
              <small class="text-muted">
                Contra él: <%= nem[:wins] %>/<%= nem[:total] %> • <%= nem[:win_rate] %>% victorias
              </small>
            <% else %>
              <small class="text-muted">Aún no hay suficientes partidos.</small>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
