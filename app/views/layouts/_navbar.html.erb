<nav class="navbar navbar-expand-lg bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      <img src="/tercer_tiempo.png" alt="Logo" width="120" height="120" class="d-inline-block align-text-top">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon" style="color: white;"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link text-white active" aria-current="page" href="<%= matches_path %>">
            <i class="fas fa-calendar-alt"></i> Partidos
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="<%= players_path %>">
            <i class="fas fa-users"></i> Jugadores
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="<%= mvp_ranking_players_path %>">
            <i class="fas fa-trophy"></i> Ranking MVP
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="<%= win_ranking_players_path %>">
            <i class="fas fa-trophy"></i> Ranking Victorias
          </a>
        </li>
      </ul>

      <!--
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to 'Cerrar sesión', destroy_user_session_path, method: :delete, class: "btn btn-light text-dark" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Iniciar sesión', new_user_session_path, class: "btn btn-light text-dark" %>
          </li>
        <% end %>
      </ul>
      -->
    </div>
  </div>
</nav>
