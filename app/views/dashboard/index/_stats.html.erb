<div class="col-md-4 mb-3">
  <%= link_to matches_path, class: "text-decoration-none text-dark" do %>
    <div class="card card-stats shadow-sm border-0">
      <div class="card-body">
        <i class="fas fa-futbol fa-2x text-primary mb-2"></i>
        <h5 class="card-title">Partidos Totales</h5>
        <p class="display-6 fw-bold"><%= Match.count %></p>
      </div>
    </div>
  <% end %>
</div>

<div class="col-md-4 mb-3">
  <%= link_to player_path(@top_mvp), class: "text-decoration-none text-dark" do %>
    <div class="card card-stats shadow-sm border-0 h-100">
      <div class="card-body text-center">
        <i class="fas fa-trophy fa-2x text-warning mb-2"></i>
        <h5 class="card-title">MVP Más Frecuente</h5>
        <% if @top_mvp.profile_photo.attached? %>
          <%= image_tag @top_mvp.profile_photo,
                        class: "rounded-circle border border-warning mb-2",
                        style: "width: 100px; height: 100px; object-fit: cover;" %>
        <% else %>
          <%= image_tag "default_avatar.png",
                        class: "rounded-circle border border-warning mb-2",
                        width: 100, height: 100 %>
        <% end %>
        <p class="fw-bold mb-0"><%= @top_mvp&.full_name || "N/A" %></p>
      </div>
    </div>
  <% end %>
</div>

<div class="col-md-4 mb-3">
  <div class="card card-stats shadow-sm border-0">
    <div class="card-body">
      <i class="fas fa-medal fa-2x text-success mb-2"></i>
      <h5 class="card-title">Top Ganadores</h5>

      <% @top_winners.each_with_index do |player, i| %>
        <p>
          <%= ["🥇", "🥈", "🥉"][i] %> <%= player.full_name %> —
          <strong><%= player.win_diff %></strong> diferencia de victorias
        </p>
      <% end %>
    </div>
  </div>
</div>